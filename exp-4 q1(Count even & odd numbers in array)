: '
 Write a function even_odd() that takes array as an argument and counts 
 number of even nos & odd numbers present inside the array.
'

function even_odd() {
    even=0
    odd=0
    for ele in ${a[@]}
    do
        if (( ele % 2 == 0 ))
        then
            even=$((even+1))
        else
            odd=$((odd+1))
        fi
    done
    echo "Even numbers = $even"
    echo "Odd numbers = $odd"
}

a=(1 2 3 4 5 6 7)
even_odd $a

# OUTPUT 
: '
Even numbers = 3
Odd numbers = 4
'
